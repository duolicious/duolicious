name: Frontend Publish
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

defaults:
  run:
    working-directory: frontend

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
    name: Publish to Cloudflare Pages
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Set DUO_WEB_VERSION
        id: vars
        run: |
          calculated_sha=$(git rev-parse --short HEAD)
          echo "DUO_WEB_VERSION=$calculated_sha" >> $GITHUB_ENV

      - uses: actions/setup-node@v4

      - run: |
          npm install
          npx patch-package
          npx expo export --platform web

          if ! patch -p0 < patches/index.html.patch
          then
            echo dist/index.html.rej was > /dev/stderr
            cat dist/index.html.rej > /dev/stderr

            echo

            echo dist/index.html was > /dev/stderr
            cat dist/index.html > /dev/stderr

            exit 1
          fi

          # The session-bridge page must be frameable by duolicious.app --
          # and only by duolicious.app -- so the apex origin can lift
          # signed-in sessions out of web.duolicious.app's localStorage
          # (see frontend/kv-storage/session-bridge.tsx). Everything else keeps
          # X-Frame-Options: DENY. Cloudflare Pages serves the page at the
          # extension-less URL and redirects the .html one to it, so only
          # the extension-less path needs the carve-out.
          cat > dist/_headers <<EOF
            /*
              X-Frame-Options: DENY
              Access-Control-Allow-Origin: ${DUO_API_URL}
            /assets/session-bridge
              ! X-Frame-Options
              Content-Security-Policy: frame-ancestors https://duolicious.app
          EOF

          # Cloudflare ignores directories containing the string 'node_modules'.
          # Expo generates directories containing the string 'node_modules'.
          # We rename the directories so that they're not ignored and get
          # uploaded.
          mv dist/assets/node_modules/* dist/assets/
          find dist/_expo/static/js/web/ -type f -print0 | xargs -0 sed -i 's/assets\/node_modules/assets/g'
        env:
          NODE_OPTIONS: --openssl-legacy-provider
          DUO_API_URL: https://api.duolicious.app
          DUO_CHAT_URL: wss://api.duolicious.app/chat
          DUO_IMAGES_URL: https://user-images.duolicious.app
          DUO_AUDIO_URL: https://user-audio.duolicious.app
          DUO_STATUS_URL: https://status.duolicious.app
          DUO_WEB_BASE_URL: https://web.duolicious.app
          DUO_GOOGLE_WEB_CLIENT_ID: 443037492722-11ai555h9htk0h5jb7dt2er5rdbpfvp1.apps.googleusercontent.com
          DUO_APPLE_WEB_CLIENT_ID: app.duolicious.web
          DUO_APPLE_REDIRECT_URI: https://api.duolicious.app/auth/apple/callback
          DUO_WEB_PUSH_VAPID_PUBLIC_KEY: BJx_Sk1NPo2v0FLbd3QSrJHe0Q5xZxUEJGVvDMUVn4DKDRaTkDeZsfkDN_hxtLYWd2gbqYt0TD_gqVDwmODoO0E
      # pages-action's `directory` is resolved from the workspace root, not
      # the job's working-directory, so it must include the subdir prefix.
      - name: Publish to web.duolicious.app
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: a73a8f703a9a1eefac56f84ca6a4c9ac
          projectName: duolicious
          directory: frontend/dist

      # The same build also serves duolicious.app: the duolicious-website
      # Pages project has the apex domain attached, and publishing the app
      # there is what replaced the old static website (see the website
      # repo's now-retired publish workflow).
      - name: Publish to duolicious.app
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: a73a8f703a9a1eefac56f84ca6a4c9ac
          projectName: duolicious-website
          directory: frontend/dist
